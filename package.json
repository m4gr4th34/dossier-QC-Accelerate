{
  "name": "dossier-qc-accelerate",
  "version": "0.0.0",
  "private": true,
  "description": "QC-Accelerate dossier — author-local tooling. render-figures seals living figures (pure Node, zero deps). Readers need no JS; CI never runs this.",
  "scripts": {
    "render-figures": "node render_figures.js"
  }
}
